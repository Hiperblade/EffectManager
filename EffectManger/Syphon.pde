// DEBUG:
class Syphon { Syphon(PApplet p, String name){ } public void send(){ } }
/*
import codeanticode.syphon.*;

class Syphon {
  SyphonServer server;
 
  PApplet p;
 
  Syphon(PApplet p, String name){
    this.p = p;
    server = new SyphonServer(p, name);
  }
 
  public void send(){
     send(p.g);
  }
  
  public void send(PGraphics g){
     server.sendImage(g);
  }
}
//*/

